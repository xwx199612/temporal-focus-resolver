# b4 audit scripts

The executable audit entrypoints live at the release root so they can be run
from an external copy without importing the canonical repository. This folder
is reserved for release-local operational scripts.
