# v0.4.2b5 Confidence Frontier Results

The version-local CLI executes official `icon_detect_v3` from encoded image bytes. The b5 artifact preserves the frozen g6 scorer and characterizes four confidence profiles with a self-reference-free release seal.

No production scorer, Omni production threshold, candidate ordering, or frozen g6 input was changed by b5.

Decisions are recorded in `decision.json`.
