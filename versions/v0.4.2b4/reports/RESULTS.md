# v0.4.2b3 Release Results

The version-local CLI executes official `icon_detect_v3` from encoded image bytes. The b3 release artifact preserves the frozen g6 scorer and provides a self-reference-free two-layer release seal.

No production scorer, Omni threshold, candidate ordering, or frozen g6 input was changed by b3.

Decisions are recorded in `decision.json`.
